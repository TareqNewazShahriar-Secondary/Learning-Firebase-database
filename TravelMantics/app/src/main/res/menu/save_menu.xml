<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/save_menu"
          android:title="@string/title__save_menu"
          app:showAsAction="always"/>
    <item android:id="@+id/cancel_menu"
          android:title="@string/cancel__save_menu"
          app:showAsAction="always" />
    <item android:id="@+id/delete_menu"
          android:title="@string/Delete__save_menu"
          app:showAsAction="never" />
</menu>